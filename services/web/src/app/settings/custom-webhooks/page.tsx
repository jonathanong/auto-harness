import { CustomWebhookSettings } from "../../../components/custom-webhook-settings.tsx";

export const dynamic = "force-dynamic";

export default function CustomWebhooksSettingsPage() {
  return <CustomWebhookSettings />;
}
